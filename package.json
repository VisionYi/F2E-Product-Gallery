{
  "name": "f2e-product-gallery",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf dist && rm -rf .cache",
    "start": "npm run clean && parcel index.html",
    "build": "npm run clean && parcel build index.html --public-url ./",
    "deploy": "gh-pages -d dist -m \"Auto-generated update\""
  },
  "author": "Visionyi - Shane Yi, Hong",
  "license": "MIT",
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not ie <= 8"
  ],
  "devDependencies": {
    "autoprefixer": "^8.6.4",
    "babel-preset-env": "^1.7.0",
    "gh-pages": "^1.2.0",
    "node-sass": "^4.9.0",
    "parcel-bundler": "^1.9.3"
  }
}
